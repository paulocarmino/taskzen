version: '3'

services:
  postgres:
    image: postgres
    ports:
      - '0.0.0.0:5432:5432'
    environment:
      POSTGRES_USER: task_manager
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: task_manager_development
